

services:
  admin-frontend:
    
    container_name: admin_frontend

    
    build:
      context: ./admin-frontend  
      dockerfile: Dockerfile     

    
    ports:
      - "8081:80" 
   
    restart: unless-stopped